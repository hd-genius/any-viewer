import { describe, it } from 'node:test'

describe('action-menu', () => {
    it.todo('should add the open attribute when it is opened')

    it.todo('should remove the open attribute when it is closed')
})
